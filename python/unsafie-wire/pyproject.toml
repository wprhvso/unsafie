[project]
name = "unsafie-wire"
version = "0.1.0"
description = "Wire contract shared by the unsafie server and the unsafie CLI"
readme = "README.md"
authors = [{ name = "wprhvso", email = "wprhvso@gmail.com" }]
requires-python = ">=3.14"
dependencies = []

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "unsafie_wire"
