- name: ntp
  ansible.builtin.include_role:
    name: geerlingguy.ntp
  vars:
    ntp_timezone: Asia/Krasnoyarsk
    ntp_manage_config: true
    ntp_servers:
      - 0.pool.ntp.org iburst
      - 1.pool.ntp.org iburst
      - 2.pool.ntp.org iburst
      - 3.pool.ntp.org iburst
