- name: build unit
  ansible.builtin.template:
    src: dumbvpn-build@.service.j2
    dest: /etc/systemd/system/dumbvpn-build@.service
    mode: "0644"
  notify: daemon reload

- name: flush
  ansible.builtin.meta: flush_handlers
