- name: installed hook binary
  ansible.builtin.stat:
    path: /usr/local/bin/dumbvpn-hook
  register: hook_bin

- name: first build
  ansible.builtin.systemd:
    name: "dumbvpn-build@{{ dumbvpn_branch }}.service"
    state: started
  when: not hook_bin.stat.exists
