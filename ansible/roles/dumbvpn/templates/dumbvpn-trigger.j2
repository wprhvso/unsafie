#!/usr/bin/env bash
set -euo pipefail

sha="${1:?commit}"
if ! [[ "$sha" =~ ^[0-9a-f]{40}$ ]]; then
    echo "not a commit sha: $sha" >&2
    exit 2
fi

systemctl reset-failed 'dumbvpn-build@*.service' 2>/dev/null || true
exec systemctl start --no-block "dumbvpn-build@${sha}.service"
