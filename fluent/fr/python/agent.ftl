agent-failure = Une erreur est survenue, réessayez.
agent-no-credentials = Toutes les sessions Opal sont indisponibles actuellement{ $when }. Réessayez plus tard.
agent-no-credentials-when = { $minutes ->
    [one] { " " }(la plus proche se libère dans ~{ $minutes } minute)
   *[other] { " " }(la plus proche se libère dans ~{ $minutes } minutes)
}
agent-stopped = Arrêté. Tout ce qui a été dit jusqu'ici reste dans le contexte.
